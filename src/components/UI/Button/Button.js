import React from 'react';

const Button = props => {
	return (
		<div className="col-md-12 text-center">
                <p><Link to="/checkout" className="btn btn-primary btn-outline btn-md">Place Order</Link></p>
        </div>
	);
};

export default Button;